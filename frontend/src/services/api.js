// DEPRECATED: zastąpione przez ../api/client.js (axios + interceptory refresh)
// oraz ../api/resources.js (funkcje endpointów). Plik zostaje jako alias, żeby
// nie psuć ewentualnych starych importów. Nowy kod używaj z `../api/*`.
import { api } from '../api/client';

export default api;
