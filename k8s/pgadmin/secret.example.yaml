# Example pgAdmin secret. Copy to `secret.yaml` and replace placeholders.
# Real file is gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: pgadmin-secret
  namespace: forum-wedkarskie
type: Opaque
stringData:
  PGADMIN_DEFAULT_EMAIL: admin@forum.local
  PGADMIN_DEFAULT_PASSWORD: REPLACE_ME
